<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Share Microservice</title>
    <style>
      body {
        background-color: #3c3c3c;
        color: white;
        padding: 1rem;
      }

      h1 {
        text-align: center;
      }

      a {
        color: white;
      }

      a:hover {
        color: #ffc0cb;
      }

      form {
        max-width: 100%;
        display: grid;
        gap: 15px;
        grid-template-columns: auto 1fr;
        max-width: 500px;
        margin: 0 auto;
        padding: 2rem;
      }

      @media (max-width: 500px) {
        h1 {
          margin-top: 5px;
        }
        form {
          padding: 10px 5px 20px 5px;
          max-width: 100%;
          gap: 5px;
        }

        body {
          padding: 5px;
        }
      }

      button {
        background-color: white;
        color: black;
        grid-column: span 2;
        padding: 5px;
        border: 1px solid white;
        border-radius: 5px;
        transition: all 300ms ease-in-out;
      }

      button:hover {
        background: linear-gradient(190deg, #ffc0cb, #fff, #ffc0cb);
      }

      input {
        padding: 5px;
        border-radius: 5px;
        border: none;
        outline: none;
      }

      input::-webkit-file-upload-button {
        background-color: #ffc0cb;
        padding: 5px 10px;
        margin-right: 8px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }

      footer {
        text-align: center;
        padding: 2rem;
      }

      .success {
        text-align: center;
        font-size: 20px;
        padding: 15px 0;
      }

      .app {
        max-width: 600px;
        margin: 2rem auto;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(5px);
        -webkit-backdrop-filter: blur(5px);
        border: 1px solid rgba(255, 255, 255, 0.3);
      }
    </style>
  </head>
  <body>
    <% if (locals.fileLink != null) { %>
    <div class="success">
      Your file is uploaded 😊
      <a href="<%= locals.fileLink %>"><%= locals.fileLink %></a>
    </div>
    <% } %>
    <div class="app">
      <h1>Secure File Sharing Microservice</h1>
      <form action="/upload" method="post" enctype="multipart/form-data">
        <label for="file">File:</label>
        <input type="file" id="file" name="file" required />
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" />
        <button type="submit">Share</button>
      </form>
    </div>
    <footer>
      <small
        >This microservice is
        <a href="https://github.com/rookiebehaviour/file-share">open-source</a>
        and coded by Jacqueline Mason 💗</small
      >
    </footer>
  </body>
</html>
