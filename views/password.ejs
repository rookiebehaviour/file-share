<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enter password</title>
    <style>
      body {
        background-color: #3c3c3c;
        color: white;
      }

      a {
        color: white;
      }

      a:hover {
        color: #ffc0cb;
      }

      form {
        display: grid;
        gap: 15px;
        grid-template-columns: auto 1fr;
        max-width: 500px;
        margin: 2rem auto;
        padding: 20px;
      }

      button {
        background-color: white;
        color: black;
        grid-column: span 2;
        padding: 5px;
        border: 1px solid white;
        border-radius: 5px;
        transition: all 300ms ease-in-out;
      }

      button:hover {
        background: linear-gradient(190deg, #ffc0cb, #fff, #ffc0cb);
      }

      input {
        padding: 5px;
        border-radius: 5px;
        border: none;
        outline: none;
      }

      .error {
        color: #ffc0cb;
        text-align: center;
        font-size: 20px;
        margin: 1rem;
      }
    </style>
  </head>
  <body>
    <% if (locals.error) { %>
    <div class="error">Password is incorrect, please re-enter password</div>
    <% } %>
    <form method="post">
      <label for="password">Password:</label>
      <input type="password" id="password" name="password" required />
      <button type="submit">Download</button>
    </form>
  </body>
</html>
